// AI integration has been removed from the application.
// This file is kept as a placeholder to avoid import errors if referenced elsewhere,
// although all references should have been removed.

export const generateReportSummary = async () => {
  console.warn("AI generation is disabled.");
  return "";
};